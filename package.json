{
  "name": "byd-main",
  "version": "1.0.0",
  "description": "This project uses environment variables to store sensitive information like API keys. For security reasons, these values are not committed to the repository.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^17.0.0",
    "express": "^5.1.0",
    "nodemailer": "^7.0.4"
  }
}
